Introduction
============

qemu + hwsim is a super handy mesh development environment.  Before proceeding,
you should read http://open80211s.org/trac/wiki/QemuAsDevelTarget.  That's
where all of the distro11s qemu support is derived from.

Launching qemu
==============

To launch qemu, simply invoke the qemu.sh script in ./board/qemu/qemu.sh
